name: Test
on:
  push:
    paths-ignore:
      - '**.md'
  workflow_dispatch:
  workflow_call:
  pull_request:
    branches:
      - "**"
    types:
      - opened
      - reopened
      - ready_for_review
      - synchronize
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  Build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24.x, 'lts/*', 'latest']
    steps:
      - uses: actions/checkout@v6

      - name: Generate lockfile
        run: npm install --package-lock-only --ignore-scripts --legacy-peer-deps

      - name: Test with Node ${{matrix.node-version}}
        uses: actions/setup-node@v6
        with:
          node-version: ${{matrix.node-version}}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts --legacy-peer-deps

      - name: Set timezone
        # Pin runner timezone so any test that touches the system tz behaves
        # deterministically. SunTimes tests pass an explicit `timezone:` and
        # don't depend on this, but it keeps future tests safe.
        run: sudo timedatectl set-timezone 'America/New_York'

      - name: Run Tests
        run: |
          npm test

      - name: Run Build
        run: |
          npm run build

      # Gate on the docs build too: typedoc.json sets treatWarningsAsErrors,
      # so a dangling/undocumented reference fails the PR instead of slipping in.
      - name: Build Docs (warnings fail the build)
        run: |
          npm run typedoc

      - name: Upload build artifact
        if: matrix.node-version == 'lts/*'
        uses: actions/upload-artifact@v7
        with:
          name: cache
          path: |
            package.json
            package-lock.json
            README.md
            LICENSE
            ./lib
